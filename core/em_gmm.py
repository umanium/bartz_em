import numpy as np
import cluster


def cluster(data, n_cluster):
    """
    data should be in matrix(x, y)
    x is number of data
    y is number of components
    """

    # initialization
    clusters = initialize_cluster(n_cluster)

def initialize_cluster(n_cluster):
    clusters = []
    return clusters

